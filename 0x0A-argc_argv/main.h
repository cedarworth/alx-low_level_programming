#ifndef "MAIN_H"
#define "MAIN_H"

#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

/**
 * _putchar - writes character c to stdou
 * check_num prototypet
 * @c: the character to be printed
 *
 * Return: on success 1
 * on error, -1 is returned
 */
int _putchar(char c);
int check_num(char *str);

#endif /*MAIN_H*/
